const StatusPill = () => {
  return (
    <span
      style={{
        fontFamily: "Verdana, Arial, sans-serif",
        fontSize: "12px",
        color: "#006600",
        fontWeight: "bold",
      }}
    >
      &#9679; Available for new opportunities
    </span>
  );
};

export default StatusPill;
